classDiagram
    class Singleton {
        -static Singleton* instance
        -Singleton()
        +static Singleton* getInstance()
    }

    class Main {
        +main() int
    }

    Main ..> Singleton : calls getInstance() for s1
    Main ..> Singleton : calls getInstance() for s2
    Singleton --> Singleton : lazily creates instance
