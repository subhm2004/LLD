# Legacy reference

`DiscountCoupon.cpp` is the original monolithic implementation kept for comparison.

Use the modular project at the parent folder (`main.cpp`, `compile.sh`) for interviews and builds.
